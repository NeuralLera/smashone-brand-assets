# `carlos-rivera` — сцены (ссылки, не копии)

Сцены **не дублируются** в папку героя: они уже лежат в банке и защищены живыми ссылками.
Здесь — перечень путей. Raw-URL = `https://raw.githubusercontent.com/smashone-corp/smashone-brand-assets/main/` + путь.

## Своих сцен в банке нет

Генерировать identity-режимом от анкера (`GEN_REF_MODE=identity`, `scripts/gen_image.py`).
Чужой кадр под этого героя не подставляется — см. раздел «Запрещено» в README героя.

